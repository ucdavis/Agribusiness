@model PersonListViewModel

@{
    ViewBag.Title = "People Index";
    ViewBag.SubPageTitle = "People Index";
}

@section SubPageNavigation
{
    <ul class="navigation">
        <li>@Html.ActionLink("Create New", "Create")</li>
    </ul>
}

@foreach (var a in Model.People)
{ 
    <fieldset>
        <legend>@a.Person.FullName</legend>

        <img src='@Url.Action("GetProfilePicture", "Person", new { id = a.Person.Id })' alt='string.Format("{0}''s Profile Picture", @a.Person.FullName)' />

        @if(a.Firm != null){
            
            <ul>
                <li>@a.Firm.Name</li>
                <li>@a.Firm.Description</li>
            </ul>
            
        }else{
            
            <text>No firm has been defined for this person</text>
            
        }
    </fieldset>
}
@model PersonViewModel

@{
    ViewBag.Title = "Create Person";
    ViewBag.SubPageTitle = "Create Person from Information Request";
}

@using (Html.BeginForm("CreatePerson", "InformationRequest", FormMethod.Post, new { enctype = "multipart/form-data", @class="form-horizontal"}) )
{
    
    @Html.ValidationSummary(false)
    
    @Html.Partial("_PersonForm")    
    
}
